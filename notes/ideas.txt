log parsing tool ideas
======================
- parse syslog format (RFC 3164) ✓
- auth.log ✓  
- apache/nginx ✓
- windows event xml ✓
- detect brute force attempts
- output to json

refactoring plan:
- base class with parse_line, parse_file, parse_stream
- each format gets its own subclass
- common LogEntry dataclass for output
- severity enum (DEBUG, INFO, WARNING, ERROR, etc)

next steps:
- brute force detection (count failed logins per IP in time window)
- privilege escalation detection
- anomaly detection (statistical?)
